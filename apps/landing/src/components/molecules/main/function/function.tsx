import FunctionView, {
  FunctionViewProps,
} from '@/components/molecules/main/function/function.view';

const Function = () => {
  const props = {} as FunctionViewProps;

  return (
    <>
      <FunctionView {...props} />
    </>
  );
};

export default Function;
