import SideBarView from '@/components/molecules/layout/side-bar.view';

const SideBar = () => {
  const props = {};

  return (
    <>
      <SideBarView {...props} />
    </>
  );
};

export default SideBar;
