export enum STATUS {
  RESERVE = 'reserve',
  IN_PROGRESS = 'inProgress',
  DONE = 'done',
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
  CANCELED = 'canceled',
}

export type VISITATION_STATUS = STATUS.RESERVE | STATUS.DONE | STATUS.PENDING;

export type TASK_STATUS =
  | STATUS.RESERVE
  | STATUS.DONE
  | STATUS.PENDING
  | STATUS.IN_PROGRESS;

export type EDUCATION_TERM_STATUS =
  | STATUS.RESERVE
  | STATUS.DONE
  | STATUS.PENDING
  | STATUS.IN_PROGRESS;

export type EDUCATION_SESSION_STATUS =
  | STATUS.RESERVE
  | STATUS.DONE
  | STATUS.PENDING;

export type JOIN_REQUEST_STATUS =
  | STATUS.PENDING
  | STATUS.APPROVED
  | STATUS.REJECTED
  | STATUS.CANCELED;
