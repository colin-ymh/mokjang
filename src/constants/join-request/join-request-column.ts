export enum JOIN_REQUEST {
  STATUS = 'status',
  USER = 'user',
  CREATED_AT = 'createdAt',
  UPDATED_AT = 'updatedAt',
  NAME = 'name',
  MOBILE_PHONE = 'mobilePhone',
  FROM_CREATED_AT = 'fromCreatedAt',
  TO_CREATED_AT = 'toCreatedAt',
}
